<%= form_with model: @artist do |f| %>
  <div>
    <%= f.label "Nom" %><br>
    <%= f.text_field :name %>
    <% @artist.errors.full_messages_for(:name).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= f.submit "Enregistrer" %>
  </div>
<% end %>
