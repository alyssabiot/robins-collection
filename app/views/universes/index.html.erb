<h1>Liste des univers</h1>

<ul>
  <% @universes.each do |universe| %>
    <li>
      <%= universe.name %> |
      <%= link_to "Modifier", edit_universe_path(universe) %> |
      <%= link_to "Supprimer", universe_path(universe),
                        method: :delete,
                        data: { confirm: "Etes-vous sûr ?" } %>

    </li>

  <% end %>
</ul>

<%= link_to "Ajouter un univers", new_universe_path %>
