<%= form_with model: @universe do |f| %>
  <div>
    <%= f.label "Nom" %><br>
    <%= f.text_field :name %>
    <% @universe.errors.full_messages_for(:name).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= f.submit "Enregistrer" %>
  </div>
<% end %>
